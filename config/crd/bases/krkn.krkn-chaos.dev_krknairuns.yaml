---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: krknairuns.krkn.krkn-chaos.dev
spec:
  group: krkn.krkn-chaos.dev
  names:
    kind: KrknAIRun
    listKind: KrknAIRunList
    plural: krknairuns
    shortNames:
    - kair
    singular: krknairun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KrknAIRunSpec defines the desired state of KrknAIRun.
            properties:
              activeDeadlineSeconds:
                default: 21600
                format: int64
                type: integer
              configYamlBase64:
                description: ConfigYAMLBase64 is the base64-encoded full krkn-ai.yaml.
                type: string
              orchestratorImage:
                type: string
              ownerUserId:
                type: string
              prometheusTokenSecretRef:
                description: PrometheusTokenSecretRef names a Secret with a "token"
                  key in the operator namespace.
                type: string
              prometheusUrl:
                type: string
              scenarioMaxRetries:
                default: 0
                type: integer
              storage:
                description: KrknAIRunStorageSpec configures the results PVC.
                properties:
                  size:
                    default: 5Gi
                    type: string
                  storageClassName:
                    type: string
                type: object
              targetClusters:
                additionalProperties:
                  items:
                    type: string
                  type: array
                minProperties: 1
                type: object
              targetRequestId:
                type: string
            required:
            - configYamlBase64
            - targetClusters
            - targetRequestId
            type: object
          status:
            description: KrknAIRunStatus defines the observed state of KrknAIRun.
            properties:
              completionTime:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failureReason:
                type: string
              orchestratorPodName:
                type: string
              phase:
                enum:
                - Pending
                - Provisioning
                - Running
                - Succeeded
                - Failed
                - Cancelled
                type: string
              pvcName:
                type: string
              scenarioRunRefs:
                items:
                  type: string
                type: array
              startTime:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
