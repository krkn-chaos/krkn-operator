---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: krkncategories.krkn.krkn-chaos.dev
spec:
  group: krkn.krkn-chaos.dev
  names:
    kind: KrknCategory
    listKind: KrknCategoryList
    plural: krkncategories
    shortNames:
    - kcat
    singular: krkncategory
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.color
      name: Color
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          KrknCategory is the Schema for the krkncategories API.
          The Kubernetes resource name identifies the category. Public and group
          visibility is stored in this resource's metadata labels; labels associating
          categories with runs are managed separately from the category definition.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KrknCategorySpec defines the desired state of a KrknCategory.
            properties:
              color:
                description: |-
                  Color is an optional hex color for UI display (e.g., "#FF5733").
                  If empty, consumers may use a default color.
                pattern: ^#[0-9A-Fa-f]{6}$|^$
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
